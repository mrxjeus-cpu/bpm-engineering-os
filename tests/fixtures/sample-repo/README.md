# Sample fixture repo (synthetic)

Repo Java hư cấu, rất nhỏ, dùng cho smoke test của `mcp-engineering`. Không chứa code thật của BPM.

Mục đích: cung cấp đủ cấu trúc (class, mapper, service, test) để kiểm tra các tool `find_symbol`, `search_code`, `get_change_context`, `validate_change_scope`.
